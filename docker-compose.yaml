services:
  cs2-analyst:
    build: .
    container_name: cs2-analyst
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - TZ=America/Sao_Paulo
      - PANDASCORE_API_TOKEN=${PANDASCORE_API_TOKEN:-}
      - ODDSPAPI_API_KEY=${ODDSPAPI_API_KEY:-}
